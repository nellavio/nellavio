export const register = async () => {
  // Startup banner is handled by src/utils/banner.ts via npm lifecycle hooks
  // This file is kept for future instrumentation needs (e.g. OpenTelemetry, Sentry)
};
